package ua.hillel.entity_relations.onetoone;

public class EmployeeProfileMappingTest {

    private Class<Employee> employeeClass;
    private Class<EmployeeProfile> employeeProfileClass;

    // TODO @Entity
    // TODO @Table(name = "employee")
    // TODO @Id @GeneratedValue
    // TODO @Column(name = "email", nullable = false, unique = true)
    // TODO @Column(name = "first_name", nullable = false)
    // TODO @Column(name = "last_name", nullable = false)

    // TODO @Entity
    // TODO @Table(name = "employee_profile")
    // TODO @Id @GeneratedValue
    // TODO @JoinColumn(name = "employee_id") @OneToOne
    // TODO @Column(name = "position", nullable = false)
    // TODO @Column(name = "department", nullable = false)
}
